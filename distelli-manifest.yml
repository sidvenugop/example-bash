svenugopalan/SimpleApp:

  Prestart:
    - ls -l /etc/lotto
  Env:
    - EXAMPLE: '"This is an example manifest environment variable."'
    
  PkgInclude:
    - application.sh
    
  Exec:
    - './application.sh'
